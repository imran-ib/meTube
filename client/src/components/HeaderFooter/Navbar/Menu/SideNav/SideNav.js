import React from "react";
import styled from "styled-components";

const SideNavStyles = styled.nav`
  margin-top: 0;
  width: 240px;
  background: #fff;
`;

function SideNav() {
  return <SideNavStyles>SideNav</SideNavStyles>;
}

export default SideNav;
