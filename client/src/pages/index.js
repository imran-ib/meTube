import Layout from "../components/layout/Layout";
import VideoListContainer from "../components/VidoeListContainer/VideoListContainer";

const Home = props => (
  <Layout>
    <VideoListContainer />
  </Layout>
);

export default Home;
